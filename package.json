{
  "name": "tradesphere",
  "private": true,
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install && cd ../dashboard && npm install",
    "build": "cd frontend && npm run build && cd ../dashboard && npm run build",
    "start": "cd backend && npm start"
  }
}